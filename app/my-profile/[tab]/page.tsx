import styled from '@emotion/styled';

export default function MyProfileContentPage({ params }: { params: { tab: string } }) {
  const { tab } = params;
  return (
    <MyProfileContentWrap>

    </MyProfileContentWrap>
  )
};

const MyProfileContentWrap = styled.div`
  
`;